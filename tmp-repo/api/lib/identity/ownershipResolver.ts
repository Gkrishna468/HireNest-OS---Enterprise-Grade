export async function resolveOwnership(globalCandidateId: string, vendorId: string): Promise<boolean> {
  // Enterprise stub: 
  // Determines if a vendor has active submission rights or if exclusivity window has expired.
  // We check candidate_variants for ownership boundaries.
  return true;
}
