rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /compliance/{organizationId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    match /resumes/{organizationId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

